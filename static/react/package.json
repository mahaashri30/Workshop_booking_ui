{
  "name": "workshop-booking-auth",
  "version": "1.0.0",
  "description": "React authentication components for Workshop Booking system",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:prod": "node build.js --prod",
    "dev": "node build.js && node -e \"console.log('Watching for changes...')\" "
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  },
  "browserslist": [
    "defaults",
    "not ie 11"
  ]
}
